#pragma once

// 최소 신장 트리 (minumum spanning tree)

#include<iostream>

namespace Union_Find
{ 
	// 백준 서로소 문제

	void getParent()	// 그래프를 주어졌을 때, 루트(부모) 노드를 찾는다
	{

	}

	void UnionParent()		// 두개의 그래프를 하나로 합친다
	{
		// 깊이 같다, 작다, 크다 <- 2개 트리 비교 -> 하나로 합칠 Best
	}

	bool Findparent()		// 같은지 아닌지 
	{
		// return (a.getParent() == b.getParent())

		// true -> 순회

	}
}